---
# NodePalette — read-only artifact palette REST service
#
# ⚠ 현재 미사용 — NodePalette는 seoy 호스트 바이너리로 실행 (K8s Pod 아님).
#   NodeVault와 같은 호스트에서 assets/ 디렉토리를 공유한다.
#   vault-index.json은 seoy 호스트 로컬 파일 → K8s Pod에서 직접 접근 불가.
#
# NodePalette를 K8s Pod로 올리려면:
#   1. NodeVault도 K8s로 이동하거나
#   2. assets/ 를 PVC/HostPath로 공유하는 방식으로 전환해야 한다.
#
# 현재 실행 방법 (seoy 호스트에서):
#   NODEPALETTE_ADDR=:8080 ./bin/nodepalette
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nodepalette
  namespace: nodepalette-system
  labels:
    app: nodepalette
  annotations:
    ignore-check.kube-linter.io/latest-tag: "replaced by deploy scripts at runtime"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nodepalette
  template:
    metadata:
      labels:
        app: nodepalette
    spec:
      containers:
        - name: nodepalette
          image: harbor.lab.local/nodevault/nodepalette:latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          env:
            - name: NODEPALETTE_ADDR
              value: ":8080"
          securityContext:
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
          volumeMounts:
            - name: tmp
              mountPath: /tmp
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 500m
              memory: 256Mi
          readinessProbe:
            httpGet:
              path: /v1/catalog/tools
              port: 8080
            initialDelaySeconds: 3
            periodSeconds: 10
      volumes:
        - name: tmp
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: nodepalette
  namespace: nodepalette-system
  labels:
    app: nodepalette
spec:
  selector:
    app: nodepalette
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
---
# Cilium HTTPRoute — NodePalette REST 엔드포인트 외부 노출
# ⚠ 현재 미사용 (Deployment 미사용이므로)
# ⚠ hostname: 아래 nip.io 주소는 stale 컨벤션이다 — Harbor는 이미
#   harbor.lab.local(CoreDNS 등록, infra-lab issue #34)로 전환됐다. 이
#   manifest를 실제로 활성화하기 전에 infra-lab과 조율해 실제 CoreDNS에
#   등록된 hostname으로 교체할 것 (추측으로 새 hostname을 넣지 않았다).
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: nodepalette-http
  namespace: nodepalette-system
spec:
  parentRefs:
    - name: lab-gateway
      namespace: harbor
  hostnames:
    - "palette.10.113.24.96.nip.io"
  rules:
    - backendRefs:
        - name: nodepalette
          port: 8080
